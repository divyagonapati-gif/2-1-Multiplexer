# 2-1-Multiplexer
A 2:1 Multiplexer is a digital circuit that selects one of two input signals and transfers the selected signal to a single output using one select line. It is commonly used for data selection, signal routing, and digital logic applications.
